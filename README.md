Docker-compose example with Nodejs
==================================

### Running manually
with 
``` npm install ```
or 
``` yarn install ```

then ``` npm start ``` or ``` nodemon ```

### Running with docker
``` docker-compose up --build ```